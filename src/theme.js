import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette : {
        primary : {
            main:"#04237F"
        },
        background : {
            default : "#fff" 
        },
        spacing : 8
    }
})
export default theme ;